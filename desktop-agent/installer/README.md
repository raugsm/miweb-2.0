# Installer

Final options: WiX, MSIX or Inno Setup.

Purpose:

- install the Windows App;
- install and register the Windows Service;
- bundle or configure the Python Cognitive Core;
- create data folders under `D:\AriadGSM` when available;
- configure startup, permissions and updater;
- avoid requiring manual PowerShell use.

