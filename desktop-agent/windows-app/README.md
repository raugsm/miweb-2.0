# Windows App

Final owner: C#/.NET WPF or WinUI.

Purpose:

- system tray application;
- start, stop and repair the local agent;
- show what the agent sees, learns, decides and fails to do;
- no visible PowerShell windows;
- communicate with the local service/API.

