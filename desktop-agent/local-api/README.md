# Local API

Purpose:

- internal communication between .NET components and Python Cognitive Core;
- prefer named pipes or localhost with a private token;
- carry only contract events;
- avoid ad hoc command-line coupling.

